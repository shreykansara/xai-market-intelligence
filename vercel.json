{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": null,
  "outputDirectory": "web",
  "framework": null,
  "rewrites": [
    {
      "comment": "The frontend calls same-origin /api/* paths. Vercel serves only static files, so proxy those to the Render service. Replace the host below with your real Render URL during deployment (DEPLOYMENT.md step 4).",
      "source": "/api/:path*",
      "destination": "https://market-intelligence-api-bt6w.onrender.com/api/:path*"
    }
  ]
}